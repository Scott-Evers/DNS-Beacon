apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dns_beacon.fullname" . }}-test-connection"
  labels:
    {{- include "dns_beacon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dns_beacon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
